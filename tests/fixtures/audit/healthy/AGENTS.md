# Healthy Fixture

A project whose context file is fully in sync with the code.

Build with `make build`; all targets are defined in the `Makefile`.

## Commands

<!-- repoctx:start -->
| Command | Source |
|---|---|
| `make build` | Makefile |
| `make clean` | Makefile |
| `make test` | Makefile |
<!-- repoctx:end -->
