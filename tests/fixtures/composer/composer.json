{
  "name": "demo/app",
  "require": {
    "php": ">=8.1",
    "monolog/monolog": "^2.0"
  },
  "require-dev": {
    "phpunit/phpunit": "^10.0"
  },
  "scripts": {
    "test": "phpunit --colors=always",
    "lint": "php-cs-fixer fix --dry-run"
  }
}
